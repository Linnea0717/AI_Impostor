[phases.install]
cmds = ["npm install --include=dev"]

[phases.build]
cmds = ["npm run build --workspace=fake-encyclopedia-client"]

[start]
cmd = "npm run start --workspace=fake-encyclopedia-server"
